import React from 'react';

import Layout from '@/components/Layout';
import Bill from '@/components/Bill';

const Cart: React.FC = () => (
  <Layout title="Cart">
    <Bill />
  </Layout>
);

export default Cart;
